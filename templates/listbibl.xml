<TEI xmlns="http://www.tei-c.org/ns/1.0">
   <teiHeader>
      <fileDesc>
         <titleStmt>
            <title>Paul-Tillich-Korrespondenz: Bibliographie</title>
         </titleStmt>
          <publicationStmt>
            <p>Publication Information</p>
         </publicationStmt>
         <sourceDesc>
            <p>Information about the source</p>
         </sourceDesc>
      </fileDesc>
   </teiHeader>
   <text>
      <body>
         <div type="index_bibls">
            <listBibl xml:id="listbibl">
            {% for x in objects %}
               <bibl xml:id="{{ x.tillich_id }}">{{ x.name }}</bibl>
            {% endfor %}
            </listBibl>
         </div>
      </body>
   </text>
</TEI>